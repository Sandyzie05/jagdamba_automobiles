# Jagdamba Automobiles website

- **`www/`** — production static site. Upload its **contents** to your host document root.
- **`app/`** — source code. Run `npm install` and `npm run build` inside `app/` to regenerate `www/`.
